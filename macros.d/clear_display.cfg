[gcode_macro CLEAR_DISPLAY]
gcode:
  {% if params.DELAY %}
    UPDATE_DELAYED_GCODE ID=_CLEAR_DISPLAY DURATION={params.DELAY}
  {% else %}
    M117
  {% endif %}

[delayed_gcode _CLEAR_DISPLAY]
gcode:
  M117

